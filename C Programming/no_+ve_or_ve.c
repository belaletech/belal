#include<stdio.h>
int main()
{
    int no;
    printf("Enter any number \n");
    scanf("%d",&no);
    if (no>0)
    {
        printf("no is positive");
         /* code */
    }
    if (no<0)
    {
       printf("no is negative"); /* code */
    }
    if(no==0)
    {
        printf("no is equal to 0");
    }
    
    
}